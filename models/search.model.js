const mongoose = require("mongoose");
const { usersSchema } = require("../db/seeds/models");

//api call to Peruneal with the search
// ERROR FINDING PLANT?
// return plant object with information
// populat plant info collection with plant object

exports.createPlantBySearch = (search) => {

}